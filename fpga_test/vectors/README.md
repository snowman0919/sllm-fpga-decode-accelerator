# FPGA Test Vectors

This directory stores exported host-generated vectors used for RTL simulation and DE10-Lite validation.

Expected files after `just vectors`:

- `q_vector.hex`
- `k_cache.hex`
- `expected_scores.csv`

The hex files use two's complement INT8 encoding.
