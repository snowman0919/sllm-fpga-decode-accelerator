# Quartus Import Mirror

This directory can contain mirrored Verilog files copied from `../../spinal/generated/` after `just spinal-generate`.

It exists to make Quartus project setup more self-contained without changing the canonical HDL generation path.
